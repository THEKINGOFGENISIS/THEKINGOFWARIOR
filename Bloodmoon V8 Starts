--//========================================================
--// BLOODMOON V8 - STATS TEST V4
--// SCROLLING FRAME
--// STRENGTHS + DURABILITY + REBIRTHS
--//========================================================

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

local Player = Players.LocalPlayer

--========================================================
-- REMOVE OLD GUI
--========================================================

pcall(function()
    local old = CoreGui:FindFirstChild("BloodMoon_Stats_Test")
    if old then
        old:Destroy()
    end
end)

--========================================================
-- FORMAT NUMBER
--========================================================

local function formatNumber(n)

    n = tonumber(n) or 0

    if n < 1000 then
        return string.format("%.0f", n)
    end

    local units = {
        {1e18, "Qi"},
        {1e15, "Qa"},
        {1e12, "T"},
        {1e9,  "B"},
        {1e6,  "m"},
        {1e3,  "k"}
    }

    for _, data in ipairs(units) do
        if n >= data[1] then

            local value = n / data[1]

            if value >= 100 then
                return string.format("%.0f%s", value, data[2])
            elseif value >= 10 then
                return string.format("%.1f%s", value, data[2])
            else
                return string.format("%.2f%s", value, data[2])
            end
        end
    end

    return tostring(n)
end

--========================================================
-- FIND VALUE
--========================================================

local function findValue(names)

    local leaderstats = Player:FindFirstChild("leaderstats")

    if leaderstats then

        for _, name in ipairs(names) do

            local obj = leaderstats:FindFirstChild(name)

            if obj and
                (obj:IsA("NumberValue") or obj:IsA("IntValue")) then

                return obj
            end
        end
    end

    for _, name in ipairs(names) do

        local obj = Player:FindFirstChild(name)

        if obj and
            (obj:IsA("NumberValue") or obj:IsA("IntValue")) then

            return obj
        end
    end

    for _, obj in ipairs(Player:GetDescendants()) do

        if obj:IsA("NumberValue") or obj:IsA("IntValue") then

            for _, name in ipairs(names) do

                if string.lower(obj.Name) == string.lower(name) then
                    return obj
                end
            end
        end
    end

    return nil
end

--========================================================
-- GET STATS
--========================================================

local function getStrength()

    local obj = findValue({
        "Strength",
        "Strengths",
        "strength",
        "strengths"
    })

    return obj and tonumber(obj.Value) or 0
end

local function getDurability()

    local obj = findValue({
        "Durability",
        "Durabilities",
        "durability",
        "durabilities"
    })

    return obj and tonumber(obj.Value) or 0
end

local function getRebirths()

    local obj = findValue({
        "Rebirth",
        "Rebirths",
        "rebirth",
        "rebirths"
    })

    return obj and tonumber(obj.Value) or 0
end

--========================================================
-- GUI
--========================================================

local GUI = Instance.new("ScreenGui")
GUI.Name = "BloodMoon_Stats_Test"
GUI.ResetOnSpawn = false
GUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
GUI.Parent = CoreGui

--========================================================
-- MAIN
--========================================================

local Main = Instance.new("Frame")
Main.Parent = GUI
Main.Size = UDim2.new(0,390,0,500)
Main.Position = UDim2.new(0.5,-195,0.5,-250)
Main.BackgroundColor3 = Color3.fromRGB(15,15,15)
Main.BorderSizePixel = 0

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0,12)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(200,0,0)
MainStroke.Thickness = 2
MainStroke.Parent = Main

--========================================================
-- TITLE
--========================================================

local Title = Instance.new("TextLabel")
Title.Parent = Main
Title.BackgroundTransparency = 1
Title.Size = UDim2.new(1,-20,0,42)
Title.Position = UDim2.new(0,10,0,4)
Title.Text = "BLOODMOON V8 - STATS"
Title.TextColor3 = Color3.fromRGB(255,40,40)
Title.TextSize = 19
Title.Font = Enum.Font.GothamBold

--========================================================
-- START BUTTON
--========================================================

local Start = Instance.new("TextButton")
Start.Parent = Main
Start.Size = UDim2.new(1,-30,0,40)
Start.Position = UDim2.new(0,15,0,48)
Start.BackgroundColor3 = Color3.fromRGB(100,0,0)
Start.Text = "START"
Start.TextColor3 = Color3.new(1,1,1)
Start.TextSize = 18
Start.Font = Enum.Font.GothamBold

local StartCorner = Instance.new("UICorner")
StartCorner.CornerRadius = UDim.new(0,8)
StartCorner.Parent = Start

--========================================================
-- STATUS
--========================================================

local Status = Instance.new("TextLabel")
Status.Parent = Main
Status.BackgroundTransparency = 1
Status.Size = UDim2.new(1,-30,0,25)
Status.Position = UDim2.new(0,15,0,91)
Status.Text = "Chưa bắt đầu đo"
Status.TextColor3 = Color3.fromRGB(180,180,180)
Status.TextSize = 13
Status.Font = Enum.Font.Gotham

--========================================================
-- SCROLLING FRAME
--========================================================

local Scroll = Instance.new("ScrollingFrame")
Scroll.Parent = Main
Scroll.Size = UDim2.new(1,-20,1,-125)
Scroll.Position = UDim2.new(0,10,0,120)

Scroll.BackgroundColor3 = Color3.fromRGB(10,10,10)
Scroll.BackgroundTransparency = 0

Scroll.BorderSizePixel = 0

Scroll.ScrollBarThickness = 5
Scroll.ScrollBarImageColor3 = Color3.fromRGB(180,0,0)

Scroll.CanvasSize = UDim2.new(0,0,0,0)

Scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
Scroll.ScrollingDirection = Enum.ScrollingDirection.Y

Scroll.VerticalScrollBarInset = Enum.ScrollBarInset.ScrollBar

local ScrollCorner = Instance.new("UICorner")
ScrollCorner.CornerRadius = UDim.new(0,8)
ScrollCorner.Parent = Scroll

--========================================================
-- CONTENT
--========================================================

local Content = Instance.new("Frame")
Content.Parent = Scroll
Content.BackgroundTransparency = 1
Content.Size = UDim2.new(1,-10,0,0)
Content.AutomaticSize = Enum.AutomaticSize.Y

--========================================================
-- LAYOUT
--========================================================

local Layout = Instance.new("UIListLayout")
Layout.Parent = Content
Layout.SortOrder = Enum.SortOrder.LayoutOrder
Layout.Padding = UDim.new(0,10)

--========================================================
-- STATS LABEL
--========================================================

local Stats = Instance.new("TextLabel")
Stats.Parent = Content
Stats.BackgroundTransparency = 1

Stats.Size = UDim2.new(1,-10,0,500)

Stats.TextXAlignment = Enum.TextXAlignment.Left
Stats.TextYAlignment = Enum.TextYAlignment.Top

Stats.TextColor3 = Color3.fromRGB(255,255,255)
Stats.TextSize = 15
Stats.Font = Enum.Font.Gotham

Stats.TextWrapped = false

Stats.Text = [[
STRENGTHS

1 Hour:  --
1 Day:   --
1 Week:  --

Strengths hiện tại: --
Strengths cài được: --

DURABILITY

1 Hour:  --
1 Day:   --
1 Week:  --

Durability hiện tại: --
Durability cài được: --

REBIRTHS

1 Hour:  --
1 Day:   --
1 Week:  --

Rebirths hiện tại: --
Rebirths cài được: --
]]

--========================================================
-- VARIABLES
--========================================================

local Started = false

local StartStrength = 0
local StartDurability = 0
local StartRebirth = 0

local LastStrength = 0
local LastDurability = 0
local LastRebirth = 0

local TotalStrengthGain = 0
local TotalDurabilityGain = 0
local TotalRebirthGain = 0

local StartTime = 0

--========================================================
-- START
--========================================================

Start.MouseButton1Click:Connect(function()

    StartStrength = getStrength()
    StartDurability = getDurability()
    StartRebirth = getRebirths()

    LastStrength = StartStrength
    LastDurability = StartDurability
    LastRebirth = StartRebirth

    TotalStrengthGain = 0
    TotalDurabilityGain = 0
    TotalRebirthGain = 0

    StartTime = os.clock()

    Started = true

    Start.Text = "RUNNING..."
    Start.BackgroundColor3 = Color3.fromRGB(0,90,0)

    Status.Text = "Đang tính lượng nhận thực tế"

end)

--========================================================
-- UPDATE
--========================================================

task.spawn(function()

    while GUI.Parent do

        task.wait(0.5)

        local currentStrength = getStrength()
        local currentDurability = getDurability()
        local currentRebirth = getRebirths()

        if Started then

            --================================================
            -- GAIN
            --================================================

            local strengthGain =
                currentStrength - LastStrength

            local durabilityGain =
                currentDurability - LastDurability

            local rebirthGain =
                currentRebirth - LastRebirth

            if strengthGain > 0 then
                TotalStrengthGain =
                    TotalStrengthGain + strengthGain
            end

            if durabilityGain > 0 then
                TotalDurabilityGain =
                    TotalDurabilityGain + durabilityGain
            end

            if rebirthGain > 0 then
                TotalRebirthGain =
                    TotalRebirthGain + rebirthGain
            end

            LastStrength = currentStrength
            LastDurability = currentDurability
            LastRebirth = currentRebirth

            --================================================
            -- TIME
            --================================================

            local elapsed =
                math.max(os.clock() - StartTime,0.5)

            --================================================
            -- RATE
            --================================================

            local strengthPS =
                TotalStrengthGain / elapsed

            local durabilityPS =
                TotalDurabilityGain / elapsed

            local rebirthPS =
                TotalRebirthGain / elapsed

            --================================================
            -- STRENGTH
            --================================================

            local strengthHour =
                strengthPS * 3600

            local strengthDay =
                strengthPS * 86400

            local strengthWeek =
                strengthPS * 604800

            --================================================
            -- DURABILITY
            --================================================

            local durabilityHour =
                durabilityPS * 3600

            local durabilityDay =
                durabilityPS * 86400

            local durabilityWeek =
                durabilityPS * 604800

            --================================================
            -- REBIRTHS
            --================================================

            local rebirthHour =
                rebirthPS * 3600

            local rebirthDay =
                rebirthPS * 86400

            local rebirthWeek =
                rebirthPS * 604800

            --================================================
            -- CAI DUOC
            --================================================

            local strengthInstall =
                math.max(
                    currentStrength - StartStrength,
                    0
                )

            local durabilityInstall =
                math.max(
                    currentDurability - StartDurability,
                    0
                )

            local rebirthInstall =
                math.max(
                    currentRebirth - StartRebirth,
                    0
                )

            --================================================
            -- DISPLAY
            --================================================

            Stats.Text =

                "STRENGTHS\n\n" ..

                "1 Hour:  +" ..
                formatNumber(strengthHour) .. "\n" ..

                "1 Day:   +" ..
                formatNumber(strengthDay) .. "\n" ..

                "1 Week:  +" ..
                formatNumber(strengthWeek) .. "\n\n" ..

                "Strengths hiện tại: " ..
                formatNumber(currentStrength) .. "\n" ..

                "Strengths cài được: +" ..
                formatNumber(strengthInstall) ..

                "\n\n" ..

                "DURABILITY\n\n" ..

                "1 Hour:  +" ..
                formatNumber(durabilityHour) .. "\n" ..

                "1 Day:   +" ..
                formatNumber(durabilityDay) .. "\n" ..

                "1 Week:  +" ..
                formatNumber(durabilityWeek) .. "\n\n" ..

                "Durability hiện tại: " ..
                formatNumber(currentDurability) .. "\n" ..

                "Durability cài được: +" ..
                formatNumber(durabilityInstall) ..

                "\n\n" ..

                "REBIRTHS\n\n" ..

                "1 Hour:  +" ..
                formatNumber(rebirthHour) .. "\n" ..

                "1 Day:   +" ..
                formatNumber(rebirthDay) .. "\n" ..

                "1 Week:  +" ..
                formatNumber(rebirthWeek) .. "\n\n" ..

                "Rebirths hiện tại: " ..
                formatNumber(currentRebirth) .. "\n" ..

                "Rebirths cài được: +" ..
                formatNumber(rebirthInstall)

        else

            Stats.Text =

                "STRENGTHS\n\n" ..

                "1 Hour:  --\n" ..
                "1 Day:   --\n" ..
                "1 Week:  --\n\n" ..

                "Strengths hiện tại: " ..
                formatNumber(currentStrength) .. "\n" ..

                "Strengths cài được: --\n\n" ..

                "DURABILITY\n\n" ..

                "1 Hour:  --\n" ..
                "1 Day:   --\n" ..
                "1 Week:  --\n\n" ..

                "Durability hiện tại: " ..
                formatNumber(currentDurability) .. "\n" ..

                "Durability cài được: --\n\n" ..

                "REBIRTHS\n\n" ..

                "1 Hour:  --\n" ..
                "1 Day:   --\n" ..
                "1 Week:  --\n\n" ..

                "Rebirths hiện tại: " ..
                formatNumber(currentRebirth) .. "\n" ..

                "Rebirths cài được: --"

        end
    end
end)

--========================================================
-- MOBILE DRAG
--========================================================

local dragging = false
local dragStart
local startPosition

Main.InputBegan:Connect(function(input)

    if input.UserInputType == Enum.UserInputType.Touch
        or input.UserInputType == Enum.UserInputType.MouseButton1 then

        dragging = true
        dragStart = input.Position
        startPosition = Main.Position

        input.Changed:Connect(function()

            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end

        end)
    end
end)

UIS.InputChanged:Connect(function(input)

    if not dragging then
        return
    end

    if input.UserInputType == Enum.UserInputType.Touch
        or input.UserInputType == Enum.UserInputType.MouseMovement then

        local delta =
            input.Position - dragStart

        Main.Position = UDim2.new(
            startPosition.X.Scale,
            startPosition.X.Offset + delta.X,

            startPosition.Y.Scale,
            startPosition.Y.Offset + delta.Y
        )
    end
end)

print("BloodMoon Stats Test V4 - ScrollingFrame Loaded") 
